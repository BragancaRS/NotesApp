<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.filipe.tasks">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/MyTheme.Dark">
        <activity android:name="com.filipe.tasks.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.filipe.tasks.AddOrEditNotes"
            android:label="Minha tarefa" />
        <activity android:name="com.filipe.tasks.About" />
        <activity android:name="com.filipe.tasks.Settings"></activity>
    </application>

</manifest>